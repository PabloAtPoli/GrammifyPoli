# Django backend
# These variables are loaded automatically from .env and .env.local.
# If both files exist, .env.local overrides .env.
SECRET_KEY=replace-with-a-secure-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Preferred for production (Render external Postgres URL)
# Keep sslmode=require in the URL query when running outside Render.
#DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://grammifypoli_user:TFhdU5AqzvholYwmLx5om0PavIWJVU6Z@dpg-d86s1f67r5hc73chma20-a.oregon-postgres.render.com/grammifypoli
# Optional local fallback values when DATABASE_URL is not set
DB_NAME=grammify_db
DB_USER=postgres
DB_PASSWORD=123
DB_HOST=localhost
DB_PORT=5432

# Frontend (optional convenience)
NEXT_PUBLIC_API_URL=http://localhost:8000
